name: phenoset_ultralytics
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.10
  - pip
  - pytorch=2.0.*
  - torchvision=0.15.*
  - torchaudio=2.0.*
  - pytorch-cuda=11.8
  - numpy
  - pandas
  - matplotlib
